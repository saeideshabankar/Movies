<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movies">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:windowSoftInputMode="adjustPan|stateHidden"
        android:name=".utils.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_movie_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_movie_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movies">
        <activity android:name=".ui.login_register.LoginRegisterNavigationActivity"/>
        <activity android:name=".ui.splash_frag.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity"/>

    </application>

</manifest>